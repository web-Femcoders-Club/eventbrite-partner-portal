{
  "name": "eventbrite-partner-portal",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev -w web",
    "dev:api": "npm run dev -w api",
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "install:all": "npm install"
  },
  "workspaces": [
    "apps/*"
  ]
}
